# Use raw GitHub URLs for large Matterport assets in production
# This keeps GitHub Pages deployment small while still serving 823MB of assets
# Note: No trailing slash - paths are appended directly
VITE_ASSETS_BASE_URL=https://raw.githubusercontent.com/Mearman/8800-blue-lick/refs/heads/main/public/assets
